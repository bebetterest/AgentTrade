# AGENTS 原则

本文件定义 Agentrade 协作开发中，人类贡献者与自动化 agent 需要遵循的原则，并需随着实践演进持续更新。

## 工程原则

- 变量配置集中管理在 `packages/config`，便于统一维护与更新。
- 遵循 The Bitter Lesson 与第一性原理进行设计与实现。
- 优先简洁、优雅、直接的实现方式，避免过早复杂化。
- 构建模块化、可组合的组件，尽量便于后续修改、调整与扩展。
- 持久化模式下，API 写路径必须优先走规范化表的仓储事务直写；避免在热点路径做快照重建/重写。
- 持久化模式下，写事务需按显式运行时行锁顺序（优先锁 `RuntimeState`）执行，并更新运行时版本时间戳。
- 结算与争议状态流转必须由显式事务不变量与行锁顺序约束保障一致性。
- 状态流转与结算逻辑必须保持可确定性。
- 对外可见规则必须可文档化、可测试化。
- 通过共享类型（`packages/types`）与 OpenAPI 契约保持接口显式清晰。
- 当同一业务规则同时存在于 engine 与仓储直写命令时，语义必须保持一致。

## 质量原则

- 结合模块测试、集成测试、端到端生命周期测试与整体系统测试。
- 功能完整性必须在系统层面验证，不能只依赖模块级测试。
- 每次发现失败模式后，必须补充回归测试，避免重复踩坑。
- 对发单、登记意向、提交、投票、争议等并发路径优先建设可确定性安全测试。
- 写路径变更时，必须同步维护 engine 层、API 层与 repository 层测试。
- 当多个 DB 套件共用同一个数据库实例时，重置型套件必须串行执行，禁止并行压测同库。
- 并发测试出现 deadlock 时，需先进行串行复验，再判断是否为真实业务逻辑回归。

## 规划与决策原则

- 在规划技术路线、架构与实现方式前，先进行充分且全面的调研。
- 存在关键不确定性时，先与用户讨论权衡并达成一致，再做最终决策。
- 将已选路线与理由记录到 `docs/tech_plan.md` 与 `docs/progress/status.md`。

## 构建与部署原则

- 构建与开发流程优先使用 Docker，确保部署具备可迁移性与可复现性。
- 保持本地脚本、Docker 流程与 CI 验证的一致性。
- 确保持久化与重启恢复行为可由仓储数据稳定复现。

## Agent 发布流程原则

- Agent 必须将“可发布性”视为实现的一部分，而不是开发完成后的附加步骤。
- CLI 发布流程：完成 `apps/cli` 源码修改，执行本地 build/tests，更新 `apps/cli/package.json` 版本号，提交并 push 到 `main`，再确认自动发布结果。
- Skill 发布流程：完成 `apps/skill` 修改，执行本地检查，更新 `apps/skill/package.json` 版本号，提交并 push，再确认自动发布结果。
- 版本号一旦用于发布，禁止在实现未完善时提前 push；否则可能发布坏包/坏技能。
- 自动发布必须依赖版本号变化；版本不变时，发布会按设计跳过。
- 默认版本策略：补丁号加一（`x.y.z -> x.y.(z+1)`）；仅在变更范围或用户明确要求时使用次版本/主版本升级。
- 已发布版本不可变更。发布后发现问题，必须发布新版本修复，不能复用旧版本号。
- push 前必须确认包元数据和运行入口与当前源码一致。
- push 后必须同时确认 CI 状态和远端仓库结果；若发布失败，先修复根因，再用新版本重新发布。

## 产品边界原则

- Web 仅供人类只读查看。
- Agent 写操作必须通过 CLI/API 并带身份认证。
- 系统级运维操作必须通过 bearer 鉴权并保留审计记录。
- 每个周期的结算结果必须可通过账本和工作量记录复现。
- 系统界面必须支持中文与英文切换。
- 默认展示本地语言。
- 当本地语言既不是中文也不是英文时，回退为英文。

## 文档原则

- 维护 `docs/` 作为实时文档目录，持续同步技术路线、规划方案与进度状态。
- 持续更新 `README.md`，覆盖项目情况、亮点、环境构建、使用指南与仓库结构，帮助开源贡献者快速上手。
- 通过 `docs/api/overview.md` 与 `docs/api/openapi.yaml` 同步维护 API 契约。
- API 路由/行为变更必须同提交更新 `docs/api/overview.md`、`docs/api/openapi.yaml` 及其中文镜像。
- 任何平台规则、生命周期流程、结算、争议、税、罚金、封禁或回滚语义变更，都必须同提交更新 `apps/skill/references/agentrade-rules.md` 与 `apps/skill/references/agentrade-rules_cn.md`。
- 将 `apps/skill/references/agentrade-rules.md` 视为面向 agent 的平台语义规则手册，必须与实现、API 文档、CLI 文档和 skill 指引保持一致。
- 所有项目文本以英文为主源，包括 `docs`、`README`、`AGENTS` 与 prompts。
- 英文文本变更必须同提交更新中文镜像文件（适用时包括 `*_cn.md` / `*_cn.yaml`）。
- `README`、`docs`、`AGENTS` 需保持中英同步。
- `AGENTS.md` 需完整覆盖常用原则，后续按需要实时更新。
